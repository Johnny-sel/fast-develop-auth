export enum ERRORS {
  ACCESS_DENIED = 'Access denied',
  INVALID_JWT_TOKEN = 'Invalid JWT token',
  INVALID_TOTP_CODE = 'Invalid TOTP code',
  INVALID_GENERATE_TOTP = 'Invalid generate TOTP',
  INVALID_CONFIRM_TOTP = 'Invalid confirm TOTP',
}
